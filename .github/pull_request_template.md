## What this changes

<!-- One or two sentences. What behaviour is different after this merges? -->

## Why

<!-- The problem being solved, not a restatement of the diff. Link the phase in
     PLANNING.md or the acceptance criterion this moves forward. -->

## How to check it

<!-- The steps a reviewer takes to see it working. "npm run dev, then..." -->

## Checklist

- [ ] `npm run verify` passes locally
- [ ] Tests cover the new behaviour, including the failure paths
- [ ] No secrets, keys or connection strings added anywhere
- [ ] `.env.example` updated if a new environment variable was introduced
- [ ] `STATUS.md` updated if this completes or advances a phase
- [ ] Public API or contract changes are reflected in `shared/`

## Notes for the reviewer

<!-- Anything deliberately left out, a trade-off taken, or a follow-up worth
     opening. Delete if there is nothing. -->
