{
  "name": "@crema/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API contract shared by the frontend and backend. One schema per concept, no duplicated types.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
